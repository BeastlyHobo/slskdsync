services:
  slskdsync:
    build: .
    container_name: slskdsync
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./app/data:/app/app/data
      - ./downloads:/downloads
      - ./library:/music
    restart: unless-stopped
